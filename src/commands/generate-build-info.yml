description: >
  generate build info file

steps:
  - run:
      name: Generate build-info file
#      environment:
#        BRANCH: "${BRANCH}"
#        COMMIT_SHA1: "${COMMIT_SHA1}"
#        BUILD_NUMBER: "${BUILD_NUMBER}"
      command: <<include(scripts/buildinfo.sh)>>
